[project]
name = "dotfiles"
revision = "1.0"

[providers]
api = "age://secrets/api.age?identity=/home/fbad/.config/sops/age/keys.txt&recipients-file=secrets/api.recipients"

[profiles.default]
MOONSHOT_API_KEY = { description = "Moonshot/Kimi key for claude-kimi", required = true, providers = ["api"] }
OPENROUTER_API_KEY = { description = "OpenRouter key for claude-openrouter", required = true, providers = ["api"] }
